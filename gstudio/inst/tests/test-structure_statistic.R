context("structure_statistic.R")


test_that("checking",{
  
  ss <- structure_statistic( "gst",0)
#   expect_that( ss, is_a("structure_statistic"))
#   expect_that( ss$mode, equals("gst") )
#   expect_that( ss$estimate, equals(0) )
#   
})