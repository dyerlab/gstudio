% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_pop.R
\name{simulate_pop}
\alias{simulate_pop}
\title{Forward-time stochastic population simulation}
\usage{
simulate_pop(
  pop,
  ngen,
  mutation = NULL,
  selfing_rate = 0,
  migration = NULL,
  census_interval = NULL,
  census_dir = NULL,
  stratum = "Population",
  verbose = TRUE
)
}
\arguments{
\item{pop}{A \code{data.frame} with at least one \code{locus} column and
a stratum column (the initial generation).}

\item{ngen}{Number of generations to simulate.}

\item{mutation}{A \code{mutation_model} object or \code{NULL} (no mutation).}

\item{selfing_rate}{Fraction of selfing on \code{[0,1]}, passed to
\code{mixed_mating()}.}

\item{migration}{A single \code{migration_event}, a list of them, or
\code{NULL} (no migration).}

\item{census_interval}{Save a snapshot every N generations, or \code{NULL}
for no census.}

\item{census_dir}{Directory in which to save \code{.rds} census files.
Created if it does not exist.}

\item{stratum}{Name of the population column (default \code{"Population"}).}

\item{verbose}{Print progress messages (default \code{TRUE}).}
}
\value{
The final-generation \code{data.frame}.
}
\description{
Runs a multi-generation forward-time simulation with optional mutation,
selfing, migration (with temporal regime changes), and census snapshots.
Each generation cycle: census, migrate, mate, mutate.
}
\examples{
\dontrun{
f <- data.frame(
  Population = rep(c("A","B"), each = 4),
  Locus = rep(c("L1","L1"), each = 4),
  Allele = rep(c("01","02"), times = 4),
  Frequency = rep(c(0.5, 0.5), times = 4)
)
pop <- make_populations(f, N = 20)
result <- simulate_pop(pop, ngen = 10, verbose = FALSE)
}
}
\author{
Rodney J. Dyer \email{rjdyer@vcu.edu}
}
